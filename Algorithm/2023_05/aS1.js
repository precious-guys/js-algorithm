const firstArray = [1, 3, 5];
const secondArray = [2, 3, 6, 7, 9];

const mergeArr = firstArray.concat(secondArray);
console.log(mergeArr);
ArrSort(mergeArr);

function ArrSort(arr) {
  const pivot = arr[0];
}
